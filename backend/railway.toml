[build]
builder = "dockerfile"

[deploy]
startCommand = "gunicorn app.main:app --config gunicorn.conf.py --bind 0.0.0.0:$PORT"
healthcheckPath = "/api/v1/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
