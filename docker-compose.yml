version: "3.9"
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - /tmp/paperiq_downloads:/tmp/paperiq_downloads
    restart: unless-stopped
